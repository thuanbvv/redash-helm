apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    name: {{ .Values.commonVolume.name }}
  name: {{ .Values.commonVolume.name }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
status: {}